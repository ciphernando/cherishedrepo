let accent = "green"
let darkBG = accent + "-950" // used in separating sections (same for light and dark mode)
let headingStyle = 'text-2xl font-bold text-primary mt-3 mb-1'
let subheading = `text-md text-primary mb-3`
let lightModeText = accent + "-950"
let lightModeBG = accent + "-50"
let darkModeText = "slate-200"
let darkModeBG = "slate-950"
 
export { headingStyle,subheading, accent, lightModeText, lightModeBG, darkModeText, darkModeBG, darkBG }
